<UserControl x:Class="Lightweight_Digital_Inventory_Management___POS_System.Views.MyPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White"
             d:DesignWidth="800" d:DesignHeight="520">

    <UserControl.Resources>
        <!-- 메뉴 항목 하나. HistoryView의 선택 카드와 같은 결이지만 세로로 쌓는 형태다. -->
        <Style x:Key="MenuItemBtn" TargetType="Button">
            <Setter Property="Background"          Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Cursor"              Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Margin"              Value="0,0,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource BorderBrushSoft}"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="20,16"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" VerticalAlignment="Center"/>
                                <Path x:Name="Chevron" Grid.Column="1"
                                      Data="M0,0 L5,5 L0,10"
                                      Stroke="{StaticResource MutedTextBrush}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      VerticalAlignment="Center" Margin="16,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"  Value="{StaticResource PrimarySoftBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="Chevron" Property="Stroke" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#DBEAFE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="My Page"
                   Style="{StaticResource PageTitle}"
                   Margin="0,0,0,20"/>

        <!-- 계정 정보 -->
        <Border Grid.Row="1" Style="{StaticResource Card}"
                Background="{StaticResource SubtleSurfaceBrush}"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Width="44" Height="44" CornerRadius="22"
                        Background="{StaticResource PrimaryBrush}"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="InitialText"
                               Foreground="White" FontSize="18" FontWeight="SemiBold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="UsernameText" Style="{StaticResource SectionLabel}" FontSize="16"/>
                    <TextBlock x:Name="RoleText" Style="{StaticResource Caption}" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 메뉴 -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="ACCOUNT" Style="{StaticResource EyebrowLabel}"/>

            <Button Style="{StaticResource MenuItemBtn}" Click="OnChangePasswordClick">
                <StackPanel>
                    <TextBlock Text="Change Password" Style="{StaticResource SectionLabel}"/>
                    <TextBlock Text="Set a new password for this account."
                               Style="{StaticResource Caption}" Margin="0,3,0,0"/>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource MenuItemBtn}" Click="OnRecoverySettingsClick">
                <StackPanel>
                    <TextBlock Text="Recovery Settings" Style="{StaticResource SectionLabel}"/>
                    <TextBlock Text="Security question and recovery email, used when you forget your password."
                               Style="{StaticResource Caption}" Margin="0,3,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <Button Grid.Row="3"
                Content="← Back"
                Style="{StaticResource SecondaryBtn}"
                HorizontalAlignment="Left" VerticalAlignment="Bottom"
                Click="OnBackClick"/>
    </Grid>
</UserControl>
