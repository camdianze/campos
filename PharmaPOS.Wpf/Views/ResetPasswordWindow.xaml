<Window x:Class="Lightweight_Digital_Inventory_Management___POS_System.Views.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Reset Password" Height="320" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TargetUsernameText" Grid.Row="0" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="New Password"/>
            <PasswordBox x:Name="NewPasswordInput"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Confirm New Password"/>
            <PasswordBox x:Name="ConfirmNewPasswordInput"/>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Cancel" Margin="0,0,5,0" Padding="0,8" Click="OnCancelClick"/>
            <Button Grid.Column="1" Content="Reset" Margin="5,0,0,0" Padding="0,8" Click="OnResetClick"/>
        </Grid>

        <TextBlock x:Name="MessageText" Grid.Row="3" Foreground="Red" Margin="0,60,0,0" TextWrapping="Wrap"/>
    </Grid>
</Window>