<UserControl x:Class="Lightweight_Digital_Inventory_Management___POS_System.Views.StockInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="500" d:DesignHeight="600">

    <Grid Width="420" Margin="20" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Stock-in" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Product Search (barcode or name, press Enter)"/>
            <TextBox x:Name="SearchBox"
                     Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                     KeyDown="OnSearchBoxKeyDown"
                     Margin="0,0,0,10"/>

            <ListBox ItemsSource="{Binding SearchResults}"
                     SelectedItem="{Binding SelectedProduct}"
                     DisplayMemberPath="ProductName"
                     MaxHeight="120"
                     Margin="0,0,0,10"/>

            <TextBlock Text="Selected Product:" FontWeight="Bold"/>
            <TextBlock Text="{Binding SelectedProduct.ProductName}" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Batch Number"/>
            <TextBox Text="{Binding BatchNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Expiry Date"/>
            <DatePicker SelectedDate="{Binding ExpiryDate}" Margin="0,0,0,10"/>

            <TextBlock Text="Stock-in Date"/>
            <DatePicker SelectedDate="{Binding StockInDate}" Margin="0,0,0,10"/>

            <TextBlock Text="Quantity"/>
            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Back" Margin="0,0,5,0" Padding="0,8" Command="{Binding CancelCommand}"/>
            <Button Grid.Column="1" Content="Save" Margin="5,0,0,0" Padding="0,8" Command="{Binding SaveCommand}"/>
        </Grid>

        <TextBlock Grid.Row="4" Text="{Binding Message}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>