<UserControl x:Class="Lightweight_Digital_Inventory_Management___POS_System.Views.InventoryStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:Lightweight_Digital_Inventory_Management___POS_System.Converters"
             mc:Ignorable="d"
             Background="White"
             d:DesignWidth="860" d:DesignHeight="600">

    <UserControl.Resources>
        <converters:UnixToDateConverter x:Key="UnixToDateConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style TargetType="TreeViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding"                    Value="0"/>
            <Setter Property="BorderThickness"            Value="0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0"
                   Text="Inventory Status"
                   Style="{StaticResource PageTitle}"
                   Margin="0,0,0,20"/>

        <!-- 필터 바 -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="13" Height="36"
                     Margin="0,0,8,0"/>

            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding AvailableExpiryFilters}"
                      SelectedItem="{Binding SelectedExpiryFilter}"
                      Height="36" Margin="0,0,8,0"/>

            <CheckBox Grid.Column="2"
                      Content="Low Stock Only"
                      IsChecked="{Binding LowStockOnly}"
                      VerticalAlignment="Center"
                      Foreground="{StaticResource TextBrush}" FontSize="13"
                      Margin="0,0,8,0"/>

            <ComboBox Grid.Column="3"
                      ItemsSource="{Binding AvailableSortOptions}"
                      SelectedItem="{Binding SelectedSortOption}"
                      Height="36"/>
        </Grid>

        <!-- TreeView -->
        <TreeView Grid.Row="2"
                  ItemsSource="{Binding GroupedItems}"
                  BorderThickness="1"
                  BorderBrush="{StaticResource BorderBrushSoft}"
                  Background="White">

            <!-- TreeViewItem 헤더는 기본적으로 내용 크기에만 맞춰져서 폭을 채우지 않는다.
                 그러면 아래 템플릿의 Grid에서 * 컬럼이 0으로 찌그러져 약품명과 'Total:'이
                 서로 붙어 보인다. Stretch를 줘야 컬럼이 제 폭을 갖는다. -->
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </TreeView.ItemContainerStyle>

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Batches}">

                    <!-- 약품 헤더 행 -->
                    <Border Padding="12,10"
                            BorderBrush="{StaticResource BorderBrushSoft}"
                            BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>

                            <!-- 약품명 + 만료임박 뱃지 -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock FontSize="14" FontWeight="SemiBold"
                                           Text="{Binding ProductName}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Border Background="#FEF3C7" CornerRadius="5"
                                        Padding="8,2" Margin="8,0,0,0"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding HasExpiringSoon, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="⚠️ Expiring" FontSize="11" FontWeight="SemiBold"
                                               Foreground="#92400E"/>
                                </Border>
                            </StackPanel>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding TotalQuantity, StringFormat='Total: {0}'}"
                                       FontSize="13" Foreground="{StaticResource MutedTextBrush}"
                                       VerticalAlignment="Center"/>

                            <TextBlock Grid.Column="2"
                                       Text="{Binding SellingPrice, StringFormat='${0:F2}'}"
                                       FontSize="13" Foreground="{StaticResource MutedTextBrush}"
                                       VerticalAlignment="Center"/>

                            <!-- HorizontalAlignment을 주지 않으면 뱃지가 70px 컬럼을 통째로 채워
                                 글자만 왼쪽에 붙은 넓적한 상자가 된다. -->
                            <Border Grid.Column="3"
                                    Background="#FEE2E2" CornerRadius="5"
                                    Padding="8,2"
                                    HorizontalAlignment="Left" VerticalAlignment="Center"
                                    Visibility="{Binding IsLowStock, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Low" FontSize="11" FontWeight="SemiBold"
                                           Foreground="{StaticResource DangerBrush}"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 배치 자식 행 -->
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SubtleSurfaceBrush}"
                                    Padding="32,8,12,8"
                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                    BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="90"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="70"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding BatchNumber}"
                                               FontSize="13" Foreground="{StaticResource TextBrush}"/>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding CurrentQuantity}"
                                               FontSize="13" Foreground="{StaticResource TextBrush}"/>

                                    <TextBlock Grid.Column="2"
                                               Text="{Binding ExpiryDate, Converter={StaticResource UnixToDateConverter}}"
                                               FontSize="13" Foreground="{StaticResource TextBrush}"/>

                                    <Border Grid.Column="3"
                                            Background="#FFE0E0" CornerRadius="4"
                                            Padding="6,2" VerticalAlignment="Center"
                                            Visibility="{Binding IsLowStock, Converter={StaticResource BoolToVis}}">
                                        <TextBlock Text="Low" FontSize="11" Foreground="{StaticResource DangerBrush}"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>

                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <!-- 하단 버튼 -->
        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="← Back"
                    Style="{StaticResource SecondaryBtn}"
                    Padding="16,0" Click="OnBackClick" Margin="0,0,8,0"/>

            <Button Grid.Column="2" Content="Stock-in"
                    Style="{StaticResource PrimaryBtn}"
                    Padding="16,0"
                    Command="{Binding StockInCommand}" Margin="0,0,8,0"/>

            <Button Grid.Column="3" Content="Adjustment"
                    Style="{StaticResource PrimaryBtn}"
                    Padding="16,0"
                    Command="{Binding AdjustmentCommand}"/>
        </Grid>

        <TextBlock Grid.Row="4"
                   Text="{Binding Message}"
                   Foreground="{StaticResource DangerBrush}"
                   TextWrapping="Wrap" FontSize="13"
                   Margin="0,8,0,0"/>
    </Grid>
</UserControl>