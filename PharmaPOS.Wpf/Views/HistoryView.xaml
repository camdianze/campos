<UserControl x:Class="Lightweight_Digital_Inventory_Management___POS_System.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White"
             d:DesignWidth="800" d:DesignHeight="450">

    <UserControl.Resources>
        <!-- 이 화면에만 있는 큰 선택 카드. 색은 App.xaml 팔레트를 참조한다.
             아래쪽 52px 여백은 카드 밑에 겹쳐 놓인 Back 버튼 자리를 비워두기 위한 것이다. -->
        <Style x:Key="HistoryCardBtn" TargetType="Button">
            <Setter Property="Background"          Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush"         Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness"     Value="1.5"/>
            <Setter Property="Cursor"              Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment"   Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20" Margin="0,0,0,52">
                            <ContentPresenter HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimarySoftBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#DBEAFE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40,32,40,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0"
                   Text="History"
                   Style="{StaticResource PageTitle}"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="Select a history type to view records."
                   Style="{StaticResource Caption}" FontSize="14"
                   Margin="0,0,0,32"/>

        <!-- 카드 2개 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sale History -->
            <Button Grid.Column="0" Style="{StaticResource HistoryCardBtn}"
                    Margin="0,0,16,0" Click="OnSaleHistoryClick">
                <Grid>
                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left"
                                Margin="32,28,0,0">
                        <TextBlock Text="Sale History"
                                   FontSize="26" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"/>
                        <TextBlock Text="View all sales transactions"
                                   Style="{StaticResource Caption}"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="🛒" FontSize="75"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,60,0,0"/>
                </Grid>
            </Button>

            <!-- Adjustment History -->
            <Button Grid.Column="1" Style="{StaticResource HistoryCardBtn}"
                    Margin="16,0,0,0" Click="OnAdjustmentHistoryClick">
                <Grid>
                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left"
                                Margin="32,28,0,0">
                        <TextBlock Text="Adjustment History"
                                   FontSize="26" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"/>
                        <TextBlock Text="View all inventory adjustments"
                                   Style="{StaticResource Caption}"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="✏️" FontSize="75"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,60,0,0"/>
                </Grid>
            </Button>
        </Grid>

        <!-- 뒤로 버튼 -->
        <Button Grid.Row="2"
                Content="← Back"
                Style="{StaticResource SecondaryBtn}"
                VerticalAlignment="Bottom" HorizontalAlignment="Left"
                Click="OnBackClick"/>
    </Grid>
</UserControl>
