<UserControl x:Class="Lightweight_Digital_Inventory_Management___POS_System.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White"
             d:DesignWidth="800" d:DesignHeight="450">

    <UserControl.Resources>
        <!-- 이 화면에만 있는 선택 카드. 색은 App.xaml 팔레트를 참조한다.
             높이를 고정해 둔다 — 화면 높이를 다 먹으면 카드 두 장이 빈 여백만
             거대하게 두르고, 정작 읽을 글자는 구석에 남는다. -->
        <Style x:Key="HistoryCardBtn" TargetType="Button">
            <Setter Property="Background"          Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush"         Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness"     Value="1.5"/>
            <Setter Property="Cursor"              Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment"   Value="Top"/>
            <Setter Property="Height"              Value="150"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimarySoftBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#DBEAFE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40,32,40,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0"
                   Text="History"
                   Style="{StaticResource PageTitle}"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="Select a history type to view records."
                   Style="{StaticResource Caption}" FontSize="14"
                   Margin="0,0,0,24"/>

        <!-- 카드 2개 -->
        <Grid Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Left" MaxWidth="720">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sale History -->
            <Button Grid.Column="0" Style="{StaticResource HistoryCardBtn}"
                    Margin="0,0,12,0" Width="340" Click="OnSaleHistoryClick">
                <Grid>
                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left"
                                Margin="22,18,0,0">
                        <TextBlock Text="Sale History"
                                   FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"/>
                        <TextBlock Text="View all sales transactions"
                                   Style="{StaticResource Caption}"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="🛒" FontSize="38"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Bottom"
                               Margin="0,0,0,16"/>
                </Grid>
            </Button>

            <!-- Stock History -->
            <Button Grid.Column="1" Style="{StaticResource HistoryCardBtn}"
                    Margin="12,0,0,0" Width="340" Click="OnStockHistoryClick">
                <Grid>
                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left"
                                Margin="22,18,0,0">
                        <TextBlock Text="Stock History"
                                   FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"/>
                        <TextBlock Text="Stock in and adjustments, in one timeline"
                                   Style="{StaticResource Caption}"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="📦" FontSize="38"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Bottom"
                               Margin="0,0,0,16"/>
                </Grid>
            </Button>
        </Grid>

        <!-- 뒤로 버튼 -->
        <Button Grid.Row="3"
                Content="← Back"
                Style="{StaticResource SecondaryBtn}"
                HorizontalAlignment="Left" Margin="0,24,0,0"
                Click="OnBackClick"/>
    </Grid>
</UserControl>
