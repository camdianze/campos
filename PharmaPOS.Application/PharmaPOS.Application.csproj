<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BCrypt.Net-Next" Version="4.2.0" />
  </ItemGroup>

  <!-- LicenseService의 검증 공개키를 시험용 키로 바꿔 끼우기 위한 것.
       운영 개인키가 저장소에 없어 테스트가 진짜 코드를 만들 수 없고,
       그러면 만료 판정을 테스트로 고정할 수 없다. -->
  <ItemGroup>
    <InternalsVisibleTo Include="PharmaPOS.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\PharmaPOS.Domain\PharmaPOS.Domain.csproj" />
  </ItemGroup>

</Project>
